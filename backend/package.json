{
  "name": "@forge/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Forge backend service — onchain job indexer + API for the marketplace.",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
